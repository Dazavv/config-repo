server:
  port: 8084
spring:
  application:
    name: file-service
  main:
    web-application-type: reactive

  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  api-docs:
    path: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: "*"